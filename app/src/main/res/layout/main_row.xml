<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:id="@+id/thumbnail_imageView"
        android:layout_marginRight="5dp" />

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/seekBar"
        android:layout_alignRight="@id/thumbnail_imageView"
        android:layout_alignLeft="@id/thumbnail_imageView"
        android:layout_alignTop="@id/thumbnail_imageView"
        android:layout_alignBottom="@id/thumbnail_imageView"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_gravity="right"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/thumbnail_imageView"
        android:layout_toEndOf="@+id/thumbnail_imageView">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/title_textView"
            tools:text="title"
            android:textStyle="bold"
            android:textColor="#000000" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="creator"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:id="@+id/creator_textView" />
    </LinearLayout>
</RelativeLayout>